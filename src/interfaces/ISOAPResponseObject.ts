export interface ISOAPResponseObject {
    ApplicableAt: Date,
    ApplicableFor: Date,
    Value: string,
    GeneratedTimeStamp: Date,
    QualityIndicator: string,
    Substituted: string,
    CreatedDate: Date
  }
