export interface ICombineDemandWeatherDataConstants {
  date: Date;
  locations: Record<string, any>;
  dayOfYear: number;
  year: number;
  dayOfMonth: number;
  dayOfWeek: number;
}

export interface IWeatherDemandDataMCConstants {
  date: Date;
  locations: Record<string, any>;
  dayOfYear: number;
  year: number;
  dayOfMonth: number;
  dayOfWeek: number;
}